//-
//- MAIN NAVBAR
//- All navigation links go here
//-

nav.navbar.navbar-inverse.fixed-to-top
  .container
    #main-navbar.main-navbar.navbar-collapse.collapse
      //- Left part of navbar
      ul.nav.navbar-nav.navbar-left
        each item in navbar
          li
            a(href=item.href, title=item.title)= item.title
      //- Right part of navbar
      ul.nav.navbar-nav.navbar-right
        li
          a(href= ROOT, title='EN') EN
        each item in translations
          li
            a(href= ROOT + item, title=item.toUpperCase())= item.toUpperCase()
    //- Header for mobile devices
    .navbar-header
      button(
        type="button",
        class="navbar-toggle collapsed",
        data-toggle="collapse",
        data-target="#main-navbar",´
        aria-expanded="false")
        span(class="icon-bar")
        span(class="icon-bar")
        span(class="icon-bar")
